# Flask API 地址（跨域 Session 须 credentials: 'include'）
# 推荐留空：自动使用与页面相同的主机名 + :5000（localhost 与 127.0.0.1 的 Cookie 不互通）
# NEXT_PUBLIC_API_BASE=

# 若固定主机名，须与浏览器地址栏一致，例如始终用 localhost 时：
# NEXT_PUBLIC_API_BASE=http://localhost:5000
